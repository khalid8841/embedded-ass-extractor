# مستخرج الترجمة المدمجة — v4

## بصراحة تامة، اقرأ هذا الجزء أول شي

هذي الإضافة ترجع لـNuvio **ملف SRT كامل وجاهز**، لأن هذا اللي يطلبه
بروتوكول الترجمات بـStremio/Nuvio — مو بث حي. وبما إن ترجمات MKV
موزعة داخل الملف من أوله لآخره، وفهرس الملف (Cues) يأشر بس لمواقع
الفيديو (keyframes) مو لمواقع الترجمة، **ما فيه طريقة حقيقية نقفز
فيها مباشرة لكل سطور الترجمة بدون قراءة تقريباً الملف كامل**.

هذا يختلف جذرياً عن مشروع Lampa subs.js، اللي يجيب الترجمة بشكل حي
أثناء التشغيل (بس الجزء القريب من نقطة المشاهدة الحالية) — استخدام
مختلف كلياً عن اللي نحتاجه هنا (ملف جاهز مسبقاً).

**يعني بصراحة:** هذي الإضافة **ما توفر نطاق ترددي بمستوى Lampa**،
وأي ادعاء عكس ذلك غير دقيق. اللي توفره فعلاً: توقف مبكر لو ما فيه
ترجمة عربية أصلاً (بدون قراءة الملف كامل بهالحالة بس).

## وش انحل فعلياً بهذي النسخة

1. **مشكلة الـCache الخاطئ:** الآن نخزن مع كل ترجمة محفوظة "بصمة"
   المصدر اللي طلعت منه. كل طلب جديد نتأكد المصدر الأفضل بالضبط لسا
   نفسه؛ لو تغير، نعيد الاستخراج بدل ما نقدم ترجمة قديمة من مصدر غلط.
2. **مشكلة الـ15 ثانية:** رفعنا وقت الانتظار لرد واحد لـ45 ثانية
   (بدل 15)، وأهم شي: لو الاستخراج ما خلص بعد، هذا **مو معناه "ما
   فيه ترجمة"** — الاستخراج يكمل بالخلفية، وأول طلب بعده (تفتح قائمة
   الترجمة مرة ثانية مثلاً) بيجيبها جاهزة من الـCache.
3. **التوقف المبكر:** لو ما فيه تراك عربي ASS/SSA أصلاً بالملف، نوقف
   فوراً بمجرد قراءة رأس الملف — ما نكمل تحميل شي.

## القيد اللي لسا موجود (وما راح يتغير)

نفس الـstream اللي تختاره بالضبط بـNuvio — هذا قيد ببروتوكول Nuvio
نفسه (ما يعطي رابط الفيديو الفعلي لإضافة الترجمة)، مو شي نقدر نحله
بالكود. الإضافة تجرب أفضل مصدر من نفس قائمتك بالترتيب، مو بالضرورة
نفس اللي اخترته يدوياً.

## اختبار محلي

```
npm install
node test-extract.js "/path/to/episode.mkv"
```

## الرفع والاستضافة

نفس خطوات النسخة السابقة:

1. ارفع `server.js`, `package.json`, `test-extract.js` على GitHub
   (Add file > Upload files).
2. render.com > New > Web Service > اختر الـrepo.
   Build Command: `npm install` — Start Command: `node server.js`
3. رابط الإعداد:
   ```json
   {"streamManifestUrl":"https://your-source-url/manifest.json","lang":"ara"}
   ```
   حوّله Base64، والرابط النهائي:
   ```
   https://your-app.onrender.com/<Base64>/manifest.json
   ```
4. ركّبه على Nuvio من إعدادات الإضافات.

## قيود إضافية تعرفها

- أول مرة لأي حلقة، الاستخراج ياخذ وقت (بحسب حجم الملف وسرعة
  الشبكة) — لأننا نقرأ الملف كامل تقريباً كما وضحنا فوق.
- Render المجاني ينام بعد فترة خمول، فأول طلب بعدها ياخذ وقت أطول.
- لو تبي توفير نطاق ترددي حقيقي بمستوى Lampa، الحل الوحيد الصحيح
  هو استخدام Lampa نفسها (تصميمها مبني أصلاً لهذا الاستخدام)، مو
  هذي الإضافة.
