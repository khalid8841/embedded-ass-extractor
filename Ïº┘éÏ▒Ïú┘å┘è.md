# مستخرج الترجمة المدمجة — v14

## v14 — البحث عن MKV بدل الاستسلام عند أول MP4

**اللي اكتشفناه من اختبارك الحقيقي:** أول مصدر ("🚀 FHD") طلع
**MP4 فعلي** (توقيعه `ftyp` وContent-Type `video/mp4`) — والكود
كان محق إنه تخطاه. المشكلة إننا كنا نفحص أول 4 مصادر بس من أصل 70.

**التعديلات:**

1. **رفعنا عدد المصادر المفحوصة من 4 إلى 20.**
2. **ترتيب ذكي للمصادر** (`rankStreamCandidates`) يجمع إشارتين:
   - بيانات ترجمة عربية مؤكدة من AIOStreams (إن وُجدت).
   - تلميح من اسم الملف: `.mkv` يرفع الترتيب، `.mp4` ينزّله.
   ملاحظة: التلميح يأثر على **الترتيب فقط** — الحكم النهائي دايماً
   من فحص البايتات الفعلي.
3. **تخطي سريع للـMP4** — نتأكد من Content-Type **وأيضاً** توقيع
   البايتات (`ftyp`)، فحتى لو السيرفر أعطى نوع محتوى غلط، نكتشفه.
   (MP4 يقدر يحمل ترجمة `mov_text`، بس **ما يحمل ASS/SSA** — وهذي
   اللي نبيها تحديداً.)
4. **يتوقف عند أول MKV فيه تراك عربي ASS/SSA** ويستخدمه.

**الاختبارات:** 69/69 ناجحة (60 منطقي + 9 تكامل)، فيها محاكاة
لنفس سيناريوك الحقيقي (أول مصدر MP4 → يتخطاه → يلقى الـMKV اللي
بعده).

## ⚠️ توقعات واقعية قبل الرفع

الاختبارات تثبت إن **المنطق** صحيح، بس ما تقدر تثبت إن عندك فعلاً
مصدر MKV فيه ترجمة عربية ASS ضمن الـ70. الاحتمالات بعد الرفع:

- **لو طلعت الترجمة** → المشكلة انحلت.
- **لو لسا فاضية** → افتح Logs بـRender وشوف السطور الجديدة:
  بتوريك بالضبط كم مصدر MP4 تم تخطيه، وكم MKV تم فحصه، وهل لقى
  تراك عربي فيها أو لا. هذا يحدد لنا الخطوة الجاية بدقة.

## خطوات الرفع (ما أقدر أرفعها بنفسي)

1. افتح `khalid8841/embedded-ass-extractor` بـGitHub.
2. استبدل: `server.js`, `lib.js`, `selftest.js`, `package.json`
   (الأربعة تغيّروا).
3. Commit — Render بيسوي Deploy تلقائي.
4. بعد "Live"، جرّب نفس رابط `/debug-aio/` أول (يتأكد كل شي شغال)،
   وبعدين جرّب التشغيل الفعلي من Nuvio.
5. لو ما ظهرت الترجمة، ابعثلي سطور Logs من Render.

---


## v13 — إصلاح مشكلة الـ404 مع AIOStreams

**السبب اللي اكتشفناه:** رابط AIOStreams المستخدم ما كان ينتهي
بـ`/manifest.json` (كان بس `/stremio/{profile}/{secret}`) — يخلي
الكود يلصق `stream/...` مباشرة بدون فاصل `/`، فينتج رابط مكسور
والسيرفر يرجع 404.

**الحل:**
1. **تحقق إجباري** — أي `streamManifestUrl` ما ينتهي حرفياً بـ
   `/manifest.json` يُرفض فوراً برسالة واضحة (بدل 404 غامض بعد
   خطوات).
2. **دالة موحدة `buildStreamUrl()`** تبني رابط الـstream من رابط
   الإعداد، وتحافظ على **كل** الأجزاء قبل `manifest.json` (البروفايل
   والسر) بدون حذف أي شي.
3. **اختبار مباشر يثبت الإصلاح:**
   `https://host/stremio/profile/secret/manifest.json` تتحول بالضبط
   لـ `https://host/stremio/profile/secret/stream/series/tt....json`
   — مُختبر فعلياً (54/54)، ونفس السلوك القديم مع Torrentio ما تأثر.

**رابط الإعداد الصحيح لـAIOStreams:**
```json
{"streamManifestUrl":"https://aiostreamsfortheweebs.midnightignite.me/stremio/{profile}/{secret}/manifest.json","lang":"ara"}
```
(بدّل `{profile}` و`{secret}` بالقيم الحقيقية من رابط Install عندك،
وتأكد ينتهي بـ`/manifest.json`).

## ⚠️ مهم: أنا ما رفعت هذا الملف GitHub ولا Render

ما عندي وصول مباشر لحسابك على GitHub أو Render — ما أقدر أرفع
أو أسوي Deploy بنفسي. لازم ترفع الملفات بنفس الخطوات المعتادة:

1. افتح مستودعك `khalid8841/embedded-ass-extractor`.
2. استبدل `server.js` و`lib.js` و`selftest.js` بالنسخ الجديدة من
   هذا الملف (نفس أسماء الملفات، بالـRoot).
3. Commit — Render المربوط بالمستودع بيسوي Deploy تلقائي.
4. بعد ما يظهر "Live" بـRender، جهّز رابط `/debug-aio/` بنفسك
   (بـBase64 الإعداد الصحيح) وجربه بالمتصفح.

---

# مستخرج الترجمة المدمجة — v12

## v12 — صفحة اختبار مخصصة لـAIOStreams (بدون لمس أي فيديو)

بناءً على طلبك: صفحة جديدة منفصلة تفحص **بس** قائمة الـstreams من
AIOStreams (أو أي مصدر بالـallowlist)، بدون أي تحميل فيديو أو فحص
Matroska أو استخراج — سؤال واحد بس: "هل الاتصال بالمصدر شغال ويرجع
بيانات قابلة للاستخدام؟"

```
https://your-app.onrender.com/<Base64>/debug-aio/series/tt9679542:4:15
```

**يرجع لك (بدون أي كشف لمفاتيح أو روابط كاملة):**
```json
{
  "httpStatus": 200,
  "looksLikeJson": true,
  "streamCount": 4,
  "streams": [
    {
      "index": 0,
      "name": "...",
      "title": "...",
      "hasUrl": true,
      "urlHostRedactedPath": "real-debrid.com",
      "hasSubtitlesMetadata": true,
      "subtitleLanguages": ["ara", "eng"],
      "confirmedArabic": true
    }
  ]
}
```

- `urlHostRedactedPath` يوريك **بس اسم الدومين** اللي يشير له رابط
  الفيديو (مثلاً `real-debrid.com`) — أبداً ما يطلع الرابط الكامل
  (ممكن يحتوي مفتاحك بالمسار).
- `confirmedArabic: true` يعني AIOStreams أكد إن هذا المصدر فيه
  ترجمة عربية مضمّنة.
- **ما يحمّل ولا بايت وحدة من أي فيديو** — بس يقرأ رد الـJSON.

هذي الصفحة تجاوبك بالضبط على الأسئلة الخمسة اللي طلبتها: حالة
الاتصال، هل الرد JSON، عدد الـstreams، هل فيها روابط، وملخص آمن عن
كل واحد — بدون أي خطوة ثانية.

**⚠️ هذا الإصدار ما ترفع GitHub ولا Render بعد — بانتظار مراجعتك.**

---

## v11 — دعم AIOStreams

بناءً على تحليلنا: قيد "نفس الـstream اللي تختاره" ثابت بغض النظر عن
المصدر (Torrentio أو AIOStreams) — هذا قيد ببروتوكول Nuvio نفسه.

**اللي انحل فعلياً:**

1. **`MANIFEST_HOST_ALLOWLIST` صار يقبل AIOStreams كمان** (جنب
   Torrentio، مو بدل عنه) — يعني تقدر تستخدم instance حقك مباشرة.
   هذا يحل على الأغلب مشكلة HTTP 403 من Torrentio، لأن AIOStreams
   حقك (مستضاف بمكان ثاني) هو اللي يكلم Torrentio، مو Render مباشرة.

2. **ترتيب أذكى للمصادر (`prioritizeKnownArabicSubtitles`).**
   AIOStreams أحياناً يعطي بيانات فعلية (مفحوصة بـFFmpeg مسبقاً عبر
   StremThru) عن تراكات الترجمة المضمّنة بكل مصدر (`subtitles` field).
   لو موجودة، نجرب المصادر المؤكد فيها ترجمة عربية **أول شي**، قبل
   نرجع للترتيب الافتراضي. المصادر بدون هذي البيانات (زي نتائج
   Torrentio العادية) تبقى بترتيبها الطبيعي بالآخر — ولا نسقط أي
   مصدر.

**رابط الإعداد الجديد** (بدّل الدومين برابط AIOStreams حقك):
```json
{"streamManifestUrl":"https://اسم-الـinstance-حقك/config/manifest.json","lang":"ara"}
```

**القيد اللي لسا موجود (وضحناه بالتحليل):** ما نقدر نضمن نفس الـstream
اللي يختاره Nuvio بالضبط، حتى مع AIOStreams — قيد ببروتوكول Nuvio.

---

آخر جولة مراجعة بناءً على فحص v9 الفعلي (تشغيل npm test بنفسك).
أربع نقاط انحلت، بدون أي تغيير بمنطق الاستخراج أو الكاش الحالي.


## تعديلان أخيران (بعد فحصك الفعلي لـv10)

1. **إصلاح رقم الإصدار** — كان لسا مكتوب v9/9.0.0 بأكثر من مكان
   بالكود (الـmanifest، رسالة السيرفر). صار الآن v10/10.0.0 بكل مكان.
2. **`PUBLIC_BASE_URL` صار إجباري فعلاً** — لو ما ضبطته بمتغيرات
   البيئة، السيرفر **يرفض يشتغل من الأساس** ويطبع رسالة خطأ واضحة،
   بدل الرجوع القديم لـHost header. جرّبت المنطق بالفحص المباشر
   للكود؛ ما قدرت أشغّل Node كامل بهذا البيئة (ما فيها اتصال إنترنت
   لتنزيل express محلياً)، بس المنطق بسيط ومباشر (`if (!x) exit(1)`)
   وسيعمل صحيح على Render حيث `npm install` سينجح فعلياً.

## الأربع نقاط (من الجولة السابقة v9→v10)

1. **إغلاق الاتصال فوراً بعد `peekStream()`.** حتى لو السيرفر
   تجاهل طلب الـRange وبدأ يرسل ملف كامل، نوقف القراءة عند 64 بايت
   ونستدعي `destroy()` على الاتصال صراحة بدل ما نسيبه معلّق.

2. **تغطية شاملة للنطاقات الخاصة/المحجوزة** — أضفنا:
   - CGNAT (`100.64.0.0/10`)
   - نطاقات TEST-NET الثلاثة (اختبار/توثيق فقط، ما تكون عناوين حقيقية)
   - نطاق Benchmarking (`198.18.0.0/15`)
   - Multicast (`224.0.0.0/4` و`ff00::/8`)
   - Reserved/Broadcast (`240.0.0.0/4`، `255.255.255.255`)
   - عنوان IPv6 غير المحدد (`::`)
   - نطاق fe80::/10 كامل (مو بس البادئة الحرفية fe80)
   - **عناوين IPv4 مغلّفة بصيغة IPv6** (`::ffff:192.168.1.1`) — يتم
     فك التغليف وفحص عنوان الـIPv4 الحقيقي بداخلها.

3. **`app.set('trust proxy', 1)`** — يخلي `req.ip` يعتمد صح على
   Header الحقيقي (`X-Forwarded-For`) بدل عنوان الـproxy نفسه، بما
   إن Render يمر عبر بروكسي عكسي بمرحلة وحدة. بدون هذا، كل المستخدمين
   خلف نفس البروكسي كانوا ممكن يتشاركون نفس حد الطلبات (Rate limit).

4. **اختبارات تكامل حقيقية بسيرفر وهمي (`integration-tests.js`)** —
   مو مجرد اختبار دوال منطقية، هذي تشغّل سيرفرات HTTP محلية فعلية
   وترسل طلبات حقيقية لها:
   - Torrentio يرجع HTML → يتكشف صح.
   - Torrentio يرجع JSON حقيقي → يتفكك صح.
   - رد بصيغة 206 مع Range حقيقي → البايتات الأولى صحيحة.
   - سيرفر يتجاهل Range ويرسل كل الملف → نتوقف عند 64 بايت ونقفل
     الاتصال بنفس الطريقة المستخدمة بالكود الحقيقي.
   - Path Traversal ضد نفس منطق `/subs/:file` عبر طلب HTTP حقيقي.
   - رفض Hostname يتحول (DNS) لعنوان خاص — بمحاكاة DNS حقيقية
     (بدون حاجة لإنترنت فعلي لهذا الاختبار بالذات).

## صدق كامل بخصوص حدود هذي الاختبارات

اختبار "رابط HTTPS عام حقيقي يعمل Redirect لعنوان خاص ويترفض" الكامل
يحتاج بنية إنترنت حقيقية (DNS + شهادات TLS فعلية) ما تنعمل محلياً
بسهولة. الاختبار البديل (محاكاة DNS) يفحص **بالضبط نفس دالة القرار**
(`resolvePublicHost`) اللي كانت راح تُستدعى بسيناريو Redirect حقيقي —
وهذا هو الجزء المهم فعلياً للحماية، موضح بالتعليقات داخل الملف نفسه.

## تشغيل الاختبارات

```
npm test
```

يشغّل:
- `selftest.js` — 36 اختبار منطقي بدون شبكة.
- `integration-tests.js` — 9 اختبارات بسيرفرات HTTP محلية حقيقية.

كلها 45/45 لازم تظهر ✅.

## هيكل الملفات

- `lib.js` — منطق خالص (إخفاء المفاتيح، كشف العناوين الخاصة، allowlist،
  تنظيف ASS، كشف JSON/Matroska).
- `server.js` — السيرفر الكامل.
- `selftest.js` — اختبارات منطقية سريعة.
- `integration-tests.js` — اختبارات بسيرفرات وهمية حقيقية.
- `test-extract.js` — اختبار استخراج من ملف MKV محلي على جهازك.

## كل ما سبق من إصلاحات (v6-v9) محفوظ بدون تغيير

إخفاء المفاتيح بالـLogs، عدم تخزين فشل مؤقت كـCache، مسار SSRF وحيد
مع Redirect يدوي مفحوص وDNS pinning، allowlist لـTorrentio، إصلاح
Path Traversal، حماية الإغراق (Rate limit + حد التزامن)، `PUBLIC_BASE_URL`،
كتابة Atomic، تنظيف الكاش التلقائي، كاش سلبي قصير مقابل عدم تخزين فشل
الشبكة، فحص توقيع Matroska، حدود الذاكرة، وتنظيف نصوص ASS.

## القيد المعماري الوحيد المتبقي (صدق، بدون حل)

ما نقدر نضمن نفس الـstream اللي يختاره المستخدم بالضبط داخل Nuvio —
قيد ببروتوكول Stremio/Nuvio نفسه، مو نقص بالكود.

## الرفع والتشغيل

1. ارفع `server.js`, `lib.js`, `package.json`, `test-extract.js`,
   `selftest.js`, `integration-tests.js` على GitHub.
2. Render.com > New > Web Service > Build: `npm install` — Start:
   `node server.js`
3. **أضف Environment Variable (إجباري الآن، السيرفر ما يشتغل بدونه):**
   ```
   PUBLIC_BASE_URL = https://اسم-تطبيقك.onrender.com
   ```
4. رابط الإعداد (Base64 لـ):
   ```json
   {"streamManifestUrl":"https://torrentio.strem.fun/realdebrid=YOUR_KEY/manifest.json","lang":"ara"}
   ```
5. جرب `/debug/series/...` ثم `/debug-stream/series/...`، وأيضاً
   `/debug-aio/series/...` (v12 — ملخص آمن لقائمة streams بدون أي
   لمسة لملف الفيديو) بالمتصفح قبل أي ربط بـNuvio.
6. لو نجحوا، ركّبها بـNuvio.
